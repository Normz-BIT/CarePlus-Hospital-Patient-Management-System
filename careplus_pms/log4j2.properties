name=PropertiesConfig

statusLoggerLevel = error
property.filename = ./logs

filters = threshold
filter.threshold.type = ThresholdFilter
filter.threshold.level = all

# RollingFileAppender - rotates every minute
appenders = rolling
appender.rolling.type = RollingFile
appender.rolling.name = RollingFileLogger
appender.rolling.fileName = ${filename}/rolling.log
appender.rolling.filePattern = ${filename}/rolling-%d{yyyy-MM-dd-HH-mm}-%i.log.gz
appender.rolling.layout.type = PatternLayout
appender.rolling.layout.pattern = [%-5level] %d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n
appender.rolling.policies.type = Policies

# Time-based policy \u2014 1 minute (driven by HH-mm in filePattern)
appender.rolling.policies.time.type = TimeBasedTriggeringPolicy
appender.rolling.policies.time.interval = 1
appender.rolling.policies.time.modulate = true

# Size-based policy (optional, uncomment to also roll on size)
# appender.rolling.policies.size.type = SizeBasedTriggeringPolicy
# appender.rolling.policies.size.size = 6kb

# Keep only 2 rolled files
appender.rolling.strategy.type = DefaultRolloverStrategy
appender.rolling.strategy.delete.type = Delete
appender.rolling.strategy.delete.basePath = ${filename}
appender.rolling.strategy.delete.maxDepth = 1
appender.rolling.strategy.delete.ifFileName.type = IfFileName
appender.rolling.strategy.delete.ifFileName.glob = rolling-*.log.gz
appender.rolling.strategy.delete.ifAccumulatedFileCount.type = IfAccumulatedFileCount
appender.rolling.strategy.delete.ifAccumulatedFileCount.exceeds = 2

# Loggers
loggers = rolling

logger.rolling.level = all
logger.rolling.additivity = false
logger.rolling.appenderRef.rolling.ref = RollingFileLogger

logger.file.name = com.careplus.client.employee.controller

# Root Logger
rootLogger.level = all
rootLogger.appenderRefs = rolling
rootLogger.appenderRef.rolling.ref = RollingFileLogger